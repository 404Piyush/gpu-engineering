# pipe-shell

The real source lives in the standalone repo:

**github.com/404Piyush/pipe-shell**

A POSIX-ish command interpreter in C11. Pipelines,
redirection, backgrounding, built-ins. 56 tests passing.

```sh
git clone https://github.com/404Piyush/pipe-shell
cd pipe-shell
make test
./pipe-shell
```

See the top-level [README](../../README.md) and
[PROJECTS.md](../../PROJECTS.md) for the full deep-dive.
