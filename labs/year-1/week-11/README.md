# Week 11: Mini-Shell (Month-3 capstone, week 1)

A four-day build of a small but real command interpreter
in C.  The final binary is `mini-shell`; this week
produces both the source tree and the standalone project
repo `404Piyush/pipe-shell`.

```
week-11/
  mini-shell/                     (the source tree, also a standalone repo)
    include/shell.h
    src/shell.c
    src/main.c
    tests/test_shell.c
    tests/test_counters.c
    tests/test_util.h
    docs/API.md
    docs/ARCHITECTURE.md
    Makefile
    README.md
  weekend-report/
    SHELL-REPORT.md
```

See `mini-shell/README.md` for usage and `notes/year-1/week-11.md`
for the journal.
